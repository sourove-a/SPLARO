'use client'

/**
 * Auth cinematic earth/video removed for storefront performance.
 * Kept as a no-op export so any stale import stays safe.
 */
export function AuthEarthBackground() {
  return null
}
