import type { ReactNode } from 'react'

/** Route shell only — sign-in ↔ create account animates inside AuthExperience. */
export default function AuthTemplate({ children }: { children: ReactNode }) {
  return children
}
