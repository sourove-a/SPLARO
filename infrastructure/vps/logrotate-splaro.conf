# SPLARO app logs — install: cp to /etc/logrotate.d/splaro
/var/log/splaro/*.log {
  daily
  rotate 7
  maxsize 50M
  missingok
  notifempty
  compress
  delaycompress
  copytruncate
  su root root
  create 0640 root root
}
