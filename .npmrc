# Root scripts call `corepack pnpm` so checks use packageManager pnpm@9.4.0.
# Keep global pnpm allowed to enter scripts; otherwise pnpm 11 exits before script start.
engine-strict=false
package-manager-strict=false
manage-package-manager-versions=true
verify-deps-before-run=false
